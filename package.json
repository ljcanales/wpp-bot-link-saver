{
  "name": "wpp-bot-link-saver",
  "private": true,
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "bootstrap": "lerna bootstrap --hoist",
    "start": "lerna run start --parallel --stream"
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}
